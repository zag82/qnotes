// collect all classes here
import PagesContext, { PagesProvider } from './pagesContext';
import PageSwitcher from './PageSwitcher';
import Page from './Page';

// export all together to access in one line
export { PagesContext, PagesProvider, Page, PageSwitcher };
